spring:
  application:
    name: seeu-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SeeU-Team/seeu-config
#        native:
#          search-locations: file://${user.home}/Desktop/seeu-config-temp
  profiles:
    active: native

server:
  port: 8888