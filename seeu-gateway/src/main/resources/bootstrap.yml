spring:
  application:
     name: seeu-gateway
  cloud:
    config:
      uri: http://localhost:8888

token:
  config:
    expiration-time: 86_400_000 # one day in millisecond
    secret: ThisIsASecret

